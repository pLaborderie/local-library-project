<%- include('components/header', { title }) %>
<p>
  Welcome to <em>LocalLibrary</em>, a very basic Express website developed as a tutorial example on the Mozilla Developer Network.
</p>
<h2 class="text-2xl my-5 font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">Dynamic content</h2>
<% if (error) { %>
  <p>Error getting content.</p>
<% } else { %>
  <p>The library has the following records:</p>
  <ul>
    <li><strong>Books: <%= data.book_count %></strong></li>
    <li><strong>Copies: <%= data.book_instance_count %></strong></li>
    <li><strong>Copies available: <%= data.book_instance_available_count %></strong></li>
    <li><strong>Authors: <%= data.author_count %></strong></li>
    <li><strong>Genres: <%= data.genre_count %></strong></li>
  </ul>
<% } %>